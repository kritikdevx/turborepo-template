{
  "name": "@repo/logger",
  "main": "./src/index.ts",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "clean": "rm -rf .turbo node_modules",
    "lint": "biome check .",
    "format": "biome format --write .",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "pino": "^9.4.0"
  },
  "devDependencies": {
    "@repo/tsconfig": "workspace:*"
  }
}
